{
  "name": "new-school-reference",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd api && npm run dev\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
